<!-- coding-standard codingKind の「レンダリング後イメージ」確定版。カスタムタグ全廃（@spec/@stack/gen-gap なし＝ソースに has-udd 固有記法ゼロ）・DocComment=言語標準スタイル・要約行=x-prompt-query 相当。ブロック= Naming / Style / DocComment / Rules。 -->

# python-hexagonal — コーディング規約

命名・スタイル・ドキュメントコメントの書き方を定める。配置・依存方向は architecture 規約、技術選択は tech-stack 規約を参照。
**ソースコードに独自記法は持ち込まない**（このスタックの標準慣習のみで書く）。

---

## 命名

| 対象 | 規約 |
|---|---|
| モジュール / ファイル | スネークケース |
| クラス | パスカルケース |
| 関数・メソッド | スネークケース |
| private（内部限定） | 先頭 `_` |
| 語彙 | 仕様（spec）のユビキタス言語に一致させる（勝手な言い換え禁止） |

---

## スタイル

| 種別 | 規約 |
|---|---|
| 必須 | 型注釈を公開関数の引数・戻り値に付ける |
| 必須 | 1関数＝1責務 |
| 禁止 | 循環 import |
| 推奨 | 早期 return で条件分岐の入れ子を避ける |

---

## ドキュメントコメント（docstring）

Google スタイル（Python の標準慣習）で書く。**このコメントは機械抽出され、コードベース全体のインデックスビューになる**——特に要約行はその要素の「読み方ガイド」として書く。

| 項目 | 規約 |
|---|---|
| スタイル | Google スタイル docstring |
| **要約行（1行目）** | 「何をするか・いつ使うか」を1行の平叙文で。**インデックスに載る前提で、検索と判断に効く語を選ぶ** |
| 本文（任意） | 責務・設計意図・使い所（why / when） |
| Args / Returns / Raises | 各項目の**意味**を書く（型は注釈が語るので繰り返さない） |
| 対象 | 公開要素（module / 公開 class / 公開 function）は必須・private は任意 |
| 独自タグ | **使用しない**（標準慣習の範囲のみ） |

```python
def render_parts(parts: list[dict], data: dict, level: int = 2) -> str:
    """宣言的 x-render の部品配列を Markdown に描画する。

    schema が宣言した部品（table/section/keyvalue 等）を block の値から
    決定的に Markdown へ変換する。書式判断はすべて部品定義側にあり、
    呼び出し側は整形ロジックを持たなくてよい。

    Args:
        parts: RenderMetaSchema に適合する部品宣言の配列。
        data: 描画対象 block の値。
        level: 小見出しの基準レベル。

    Returns:
        描画済み Markdown（部品間は空行区切り）。

    Raises:
        RenderError: 部品種別が未知、または from 参照が解決できない場合。
    """
```

---

## 決定ルール

| 種別 | 規約 |
|---|---|
| 必須 | 公開要素に要約行つき docstring を書く |
| 禁止 | コードから導出できる情報を docstring に書く（型の列挙・実装手順の逐語説明） |
| 禁止 | 仕様と異なる語彙での命名（ユビキタス言語の言い換え） |
| 推奨 | コメントは「なぜ」を書く（「何を」は命名と要約行が表現する） |
